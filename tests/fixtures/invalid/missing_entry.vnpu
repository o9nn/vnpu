// vNPU Test Fixture (INVALID): isolate missing 'entry' declaration
// Expected: semantic error "isolate missing 'entry' declaration: 'iso0'"
vnpu v1;

device cpu0 { kind=cpu; }

tensor a : f32[4] @cpu0;

kernel k0 = aten.relu(a) -> a;

graph g0 { k0; }

isolate iso0 {
  membrane=inner;
  ports { input: Intent; }
}
