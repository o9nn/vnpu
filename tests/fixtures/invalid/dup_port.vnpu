// vNPU Test Fixture (INVALID): duplicate port name within an isolate
// Expected: semantic error "duplicate port name in isolate: 'input'"
vnpu v1;

device cpu0 { kind=cpu; }

tensor a : f32[4] @cpu0;

kernel k0 = aten.relu(a) -> a;

graph g0 { k0; }

isolate iso0 {
  membrane=inner;
  entry g0;
  ports {
    input: Intent;
    input: Evidence;
  }
}
